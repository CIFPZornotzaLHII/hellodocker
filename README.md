# hellodocker

Este repositorio contiene un archivo docker-compose.yml que define dos servicios: WordPress y MySQL. 

Tu tarea es escribir aquí, en formato markdown el contenido del archivo docker-compose.yml (usando un bloque de código). 

Identenificar los distintos elemenetos de un fichero docker-compose:
- Servicios
- Imagenes
- etc

Intenta averiguar que significa cada uno y responde en una o dos lineas.
